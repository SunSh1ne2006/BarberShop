// Можно оставить пустым или добавить простой код, если в будущем понадобится интерактив.
// Сейчас страница статична, бургер-меню не требуется.

document.addEventListener('DOMContentLoaded', () => {
    // Здесь можно инициализировать что-то, например, плавный скролл к якорям (если кнопки поведут на секции)
    // Но сейчас кнопки-ссылки ведут на "#", поэтому код не обязателен.
});